# This file is required by ESPHome to detect this folder as a valid custom component.
# It allows external_components to pull this component successfully.

# While we do not define a Python-based platform (like a sensor or switch),
# this component is registered via YAML as a custom_component, with C++ logic only.

def setup_platform():
    # Dummy function to ensure this file is parsed and linked.
    # Not used directly.
    pass
